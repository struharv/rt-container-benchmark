global threads

probe scheduler.cpu_on
{
    printf("ON %d %lu %d %s\n", tid(), gettimeofday_ns(), cpu(), execname());
}

probe scheduler.cpu_off
{
    printf("OFF %d %lu %d %s\n", tid(), gettimeofday_ns(), cpu(), execname());
}
